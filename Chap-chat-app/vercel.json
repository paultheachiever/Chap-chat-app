{
  "version": 2,
  "builds": [
    { "src": "server-side/server.js", "use": "@vercel/node" },
    { "src": "frontend/dist/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server-side/server.js" },
    { "src": "/(.*)", "dest": "frontend/dist/$1" }
  ]
}
